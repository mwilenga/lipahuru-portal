# Local development
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Production (set before `npm run build` — value is baked into the client bundle)
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com/api
