# Local development
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# For server deploys use:
#   .env.sandbox  → npm run deploy:sandbox  (port 3002)
#   .env.prod     → npm run deploy:prod     (port 3001)
# See .env.sandbox.example and .env.prod.example
